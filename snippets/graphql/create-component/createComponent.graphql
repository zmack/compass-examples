mutation createComponent(
    $cloudId: ID!
    $componentDetails: CreateCompassComponentInput!
) {
    compass {
        createComponent(cloudId: $cloudId, input: $componentDetails) {
            success
            errors {
                message
            }
            componentDetails {
                id
                name
                typeId
            }
        }
    }
}
